#include <bits/stdc++.h>
#include "number.h"

using namespace std;

int main() {
    freopen("input.txt", "r", stdin);
    //freopen("output.txt", "w", stdout);

    Number n;

    cout << n;

    cout << '\n'; return 0;
}